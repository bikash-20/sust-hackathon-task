{
  "version": 2,
  "installCommand": "npm install --prefix frontend-pwa",
  "buildCommand": "npm run build --prefix frontend-pwa",
  
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
